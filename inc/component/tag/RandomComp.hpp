#ifndef __RANDOM_COMP__
#define __RANDOM_COMP__

/**
 * This tag should be added to any entity which rather than providing certain values, just uses random values instead
 */
struct RandomComp
{ };

#endif