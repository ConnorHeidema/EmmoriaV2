#ifndef __INTERACTABLE_COMP__
#define __INTERACTABLE_COMP__

#include <string>

/**
 * This component should be added to any entity which may be interacted with. This is the observer of the interaction
 */
struct InteractableComp
{};

#endif